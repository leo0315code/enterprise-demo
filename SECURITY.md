# 安全说明

本文档介绍了企业官网系统的安全特性、安全配置以及安全最佳实践。

## 安全特性

### 已实现的安全措施

1. **输入验证与过滤**
   - 所有表单输入都经过验证和清理
   - 文件上传使用白名单验证（基于 MIME 类型检测）
   - SQL 查询使用预处理语句，防止 SQL 注入

2. **认证与授权**
   - 后台登录使用加密密码存储（bcrypt）
   - 登录尝试限制（防暴力破解）
   - 会话管理使用 Laravel 内置安全机制

3. **内容安全策略 (CSP)**
   - 限制资源加载来源，防止 XSS 攻击
   - 配置策略：`default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self'; frame-ancestors 'self'; base-uri 'self'; form-action 'self';`

4. **HTTP 安全头**
   - `X-Content-Type-Options: nosniff` - 防止 MIME 类型嗅探
   - `X-Frame-Options: SAMEORIGIN` - 防止点击劫持
   - `Referrer-Policy: strict-origin-when-cross-origin` - 保护隐私
   - `Strict-Transport-Security` - 强制 HTTPS 连接
   - `Permissions-Policy` - 限制敏感 API 访问

5. **CSRF 保护**
   - 使用 Laravel 内置 CSRF 令牌保护表单提交
   - 防止跨站请求伪造攻击

6. **XSS 防护**
   - Blade 模板引擎自动转义输出
   - 富文本编辑器内容过滤

7. **速率限制**
   - 登录尝试限制：5次/分钟
   - 留言提交限制：3次/分钟
   - 防止垃圾信息和暴力攻击

8. ** honeypot 验证**
   - 联系表单使用隐藏字段检测机器人
   - 机器人提交会被静默丢弃

### 数据保护

1. **数据库安全**
   - 敏感信息不直接存储在数据库中
   - 使用加密连接（如适用）

2. **文件上传安全**
   - 仅接受特定类型的图像文件（jpg, jpeg, png, gif, webp）
   - 文件扩展名基于 MIME 类型推断，不信任客户端提供的扩展名
   - 上传文件存储在受保护的目录中

3. **备份策略**
   - 定期备份数据库和重要文件
   - 备份文件加密存储

## 安全配置建议

### 服务器配置

1. **HTTPS 配置**
   - 启用 TLS 1.2 或更高版本
   - 使用强加密套件
   - 配置 HSTS 头（已在应用中实现）

2. **防火墙配置**
   - 限制不必要的端口访问
   - 配置 IP 白名单（如适用）

3. **PHP 安全配置**
   ```ini
   expose_php = Off
   allow_url_include = Off
   cgi.fix_pathinfo = 0
   opcache.validate_timestamps = 0  ; 生产环境中禁用时间戳验证
   ```

### 应用配置

1. **环境变量安全**
   - 将敏感配置存储在环境变量中
   - 不要在代码中硬编码敏感信息
   - 确保 `.env` 文件不在 Web 可访问目录中

2. **调试模式**
   - 生产环境中设置 `APP_DEBUG=false`
   - 避免泄露敏感信息

3. **日志安全**
   - 定期轮换日志文件
   - 敏感信息不应记录在日志中
   - 限制日志文件访问权限

## 安全最佳实践

### 密码策略
- 使用强密码（至少 12 位，包含大小写字母、数字和特殊字符）
- 定期更换密码
- 不要在多个系统中重复使用相同密码

### 用户权限
- 遵循最小权限原则
- 定期审查用户权限
- 及时删除不再需要的用户账户

### 定期安全检查
- 定期更新依赖包
- 监控安全漏洞公告
- 定期进行安全扫描

### 监控和日志
- 监控异常登录尝试
- 记录安全相关事件
- 设置警报机制

## 安全应急响应

### 发现安全漏洞
1. 立即隔离受影响的系统
2. 评估漏洞影响范围
3. 实施临时缓解措施
4. 开发和部署修复程序
5. 通知相关人员

### 数据泄露处理
1. 立即停止数据泄露
2. 评估泄露数据范围
3. 通知相关监管机构（如适用）
4. 通知受影响的用户
5. 实施预防措施防止再次发生

## 安全更新

### 依赖包更新
- 定期检查和更新 PHP 依赖
- 关注 Laravel 安全更新
- 测试更新后的功能完整性

### 安全补丁
- 及时应用操作系统安全补丁
- 更新 Web 服务器软件
- 更新数据库管理系统

## 联系信息

如发现安全漏洞，请通过以下方式联系：
- 邮箱：[安全联系邮箱]
- 电话：[安全联系电话]

## 审计日志

系统会记录以下安全相关事件：
- 用户登录/登出
- 密码更改
- 权限更改
- 文件上传
- 管理员操作